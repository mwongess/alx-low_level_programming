#include "main.h"
/**
 * main - check code
 *
 * Return: Always 0.
 */
int main(void)
{
	if (n > 0)
{
	printf("%i is positive\n", n);
}
else if (n == 0)
{
	printf("%i is zero\n", n);
}
else if (n < 0)
{
	printf("%i is negative\n", n);
}
	return (0);
}
